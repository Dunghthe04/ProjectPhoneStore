mixin tree(record,level=1,parrent_id="")
  each item in record
    - const prefix=  Array(level+1).join("-- ")
    option(value=item.id selected=(item.id==parrent_id?true:false)) #{prefix}#{item.title}
    if item.children && item.children.length >0
      +tree(item.children,level+1,parrent_id)